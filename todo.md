# BizSync 프로젝트 TODO 항목

## 🐛 버그 검토

1. WebSocket 연결 끊김 시 세션 정리 누락
2. 다단계 결재 시 중간 단계 스킵 가능 여부
3. 동시 결재 승인 시 중복 처리 가능성
4. JWT 토큰 만료 시 자동 갱신 미구현
5. 파일 업로드 용량 제한 검증 누락
6. 알림 발송 실패 시 재시도 로직 없음
7. 트랜잭션 롤백 시 캐시 동기화 문제
8. N+1 쿼리 발생 가능 엔티티 조회
9. 예외 처리 시 민감 정보 노출 가능성
10. 페이징 처리 시 totalCount 성능 이슈

## 🔧 리팩토링

11. 반복되는 권한 체크 로직 AOP로 분리
12. Service 계층 비즈니스 로직 과다 (클래스 분리)
13. 매직 넘버/문자열 상수화
14. DTO 변환 로직 중복 (MapStruct 도입)
15. 응답 형식 통일 (ResponseEntity 래퍼)
16. 예외 처리 전략 통일 (GlobalExceptionHandler)
17. 테스트 코드 커버리지 부족
18. 로깅 전략 미흡 (로그 레벨, 형식)
19. 설정값 하드코딩 (application.yml 분리)
20. 메서드 길이 과다 (SRP 위반)

## 🔐 보안 강화

21. SQL Injection 방어 (MyBatis 파라미터 검증)
22. XSS 방어 (입력값 sanitization)
23. CSRF 토큰 검증
24. 비밀번호 암호화 강도 (BCrypt rounds)
25. API 요청 Rate Limiting
26. 민감 정보 로깅 방지
27. CORS 설정 제한적 허용
28. 파일 업로드 확장자 화이트리스트
29. JWT Secret Key 환경변수 분리
30. DB 접속 정보 암호화

## ⚡ 기능 추가 (Redis + 멀티스레드 + Batch)

31. Redis 환경 세팅
32. 프로젝트 목록 조회 캐싱
33. 사용자 권한 캐싱
34. WebSocket 세션 Redis 전환
35. 결재 중복 방지 (분산 락)
36. 문서 동시 편집 방지 (분산 락)
37. 대량 알림 발송 멀티스레드
38. Excel 다운로드 비동기 처리
39. 만료 프로젝트 아카이빙 배치
40. 월간 통계 집계 배치
41. 미확인 알림 자동 삭제 배치
42. 온라인 사용자 현황 추적
43. 캐시 무효화 전략 구현
44. 배치 스케줄링 설정
45. 성능 측정 및 모니터링

## 📝 문서화

46. API 문서 작성 (Swagger/Spring REST Docs)
47. ERD 최신화
48. 아키텍처 다이어그램
49. 배치 스케줄 표
50. 성능 개선 수치 정리
